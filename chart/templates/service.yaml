apiVersion: v1
kind: Service
metadata:
  name: {{ include "gke-demo-app.fullname" . }}
  labels:
    {{- include "gke-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "gke-demo-app.selectorLabels" . | nindent 4 }}
